import { averageVocabulary } from 'promptr/helpers/average-vocabulary';
import { module, test } from 'qunit';

module('Unit | Helper | average vocabulary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageVocabulary([42]);
  assert.ok(result);
});
