import { averageSpelling } from 'promptr/helpers/average-spelling';
import { module, test } from 'qunit';

module('Unit | Helper | average spelling');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageSpelling([42]);
  assert.ok(result);
});
