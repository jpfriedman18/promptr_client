import { averageCreativity } from 'promptr/helpers/average-creativity';
import { module, test } from 'qunit';

module('Unit | Helper | average creativity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageCreativity([42]);
  assert.ok(result);
});
