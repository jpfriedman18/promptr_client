import { averageStructure } from 'promptr/helpers/average-structure';
import { module, test } from 'qunit';

module('Unit | Helper | average structure');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageStructure([42]);
  assert.ok(result);
});
